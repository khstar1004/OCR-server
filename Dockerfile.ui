FROM python:3.12-slim-bookworm

ARG ACONG_BUILD_VERSION=local
ARG ACONG_BUILD_DATE=unknown

ENV DEBIAN_FRONTEND=noninteractive \
    PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    APP_ENV=docker \
    API_PREFIX=/api/v1 \
    WATCH_DIR=/data/watch \
    DATA_DIR=/data/runtime \
    DATABASE_URL=sqlite:////data/runtime/db/news_ocr.db \
    AUTO_CREATE_TABLES=true \
    INPUT_ROOT=/data/watch \
    OUTPUT_ROOT=/data/runtime/output \
    MODELS_ROOT=/models \
    OCR_BACKEND=chandra \
    OCR_SERVICE_URL=http://a-cong-ocr-service:5000 \
    OCR_SERVICE_MODE=native \
    OCR_SERVICE_TIMEOUT_SEC=0 \
    OCR_SERVICE_POLL_INTERVAL_SEC=2.0 \
    OCR_OFFLINE=true \
    OCR_DEVICE=remote \
    HF_HUB_DISABLE_TELEMETRY=1

LABEL org.opencontainers.image.title="Army-OCR UI" \
      org.opencontainers.image.description="Lightweight Army-OCR web and playground image" \
      org.opencontainers.image.version="${ACONG_BUILD_VERSION}" \
      org.opencontainers.image.created="${ACONG_BUILD_DATE}"

WORKDIR /app

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
        ca-certificates \
        curl \
        libglib2.0-0 \
        libgl1 \
        libgomp1 \
        libsm6 \
        libxext6 \
        libxrender1 \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.ui.txt /app/requirements.ui.txt
RUN python -m pip install --upgrade pip setuptools wheel \
    && python -m pip install -r /app/requirements.ui.txt

RUN mkdir -p /data/watch /data/runtime/db /data/runtime/output /models

COPY app /app/app
COPY templates /app/templates
COPY static /app/static
COPY docs /app/docs
COPY README.md /app/README.md
COPY docker-compose.yml /app/docker-compose.yml
COPY .env.example /app/.env.example

VOLUME ["/data/watch", "/data/runtime"]

EXPOSE 5000

CMD ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "5000"]
